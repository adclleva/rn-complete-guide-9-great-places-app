class Place {
  constructor(id, title, imageUri, address, longitude, latitude) {
    this.id = id;
    this.title = title;
    this.imageUri = imageUri;
    this.address = address;
    this.longitude = longitude;
    this.latitude = latitude;
  }
}

export default Place;
